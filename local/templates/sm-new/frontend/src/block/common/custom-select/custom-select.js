import $ from 'jquery';

$(function () {
	$('.b-custom-select select').each(function () {
		// let theme = $(this).data('theme');
		// new Select({
		// 	el: $(this)[0],
		// 	className: theme ? 'select-theme-default ' + theme : 'select-theme-default'
		// }).on('open', function() {
		// 	$(this.content).css('width', $(this.target).width());
		// });
	});
});
